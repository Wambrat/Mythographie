{% extends 'base.html.twig' %}

{% block title %}StripBoxe{% endblock %}

{% block body %}
    <h1>StripBoxe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ strip_boxe.id }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ strip_boxe.body }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ strip_boxe.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('strip_boxe_index') }}">back to list</a>

    <a href="{{ path('strip_boxe_edit', {'id': strip_boxe.id}) }}">edit</a>

    {{ include('strip_boxe/_delete_form.html.twig') }}
{% endblock %}
