<form method="post" action="{{ path('strip_boxe_delete', {'id': strip_boxe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ strip_boxe.id) }}">
    <button class="btn">Delete</button>
</form>
